{
    "scripts": {
        "build": "web-ext build",
        "start": "web-ext run",
        "start:firefox": "web-ext run --source-dir ./",
        "lint": "web-ext lint"
    },
    "devDependencies": {
        "web-ext": "^8.10.0"
    }
}
